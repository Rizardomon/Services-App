import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:services_app/app/modules/collect/collect_module.dart';

void main() {
  initModule(CollectModule());
  // CollectController collect;
  //
  setUp(() {
    //     collect = CollectModule.to.get<CollectController>();
  });

  group('CollectController Test', () {
    //   test("First Test", () {
    //     expect(collect, isInstanceOf<CollectController>());
    //   });

    //   test("Set Value", () {
    //     expect(collect.value, equals(0));
    //     collect.increment();
    //     expect(collect.value, equals(1));
    //   });
  });
}
